services:
  - type: web
    name: islamic-reels-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python your_bot_file.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
